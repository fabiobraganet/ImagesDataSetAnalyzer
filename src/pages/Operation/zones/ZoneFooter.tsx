import React from 'react';
import { Layout } from 'antd';

const { Footer } = Layout;

const ZoneFooter: React.FC = () => (
  <Footer>
    <Layout className="base">Operation Footer</Layout>
  </Footer>
);

export default ZoneFooter;
